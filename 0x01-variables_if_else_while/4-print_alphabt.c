#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */

int main(void)
{
	char k;

	k = 'a';
	while
	(k <= 'a') {
		if ((k != 'q' && k != 'e') && k <= 'z')
			putchar(k);
		k++;
	}
	putchar('\n');
	return (0);
}
